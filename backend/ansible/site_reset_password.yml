##
# ansible-playbook -i site_reset_pasword.yml
---
- name: Reset password
  hosts: all
  gather_facts: false
  remote_user: root

  vars_files:
    - vars/main.yml

  # pre_tasks:
  #   - include_role:
  #       name: etc-git
  #       tasks_from: commit.yml
  #     vars:
  #       commit_message: "Ansible {{ ansible_version.full }} pre-run playbook.yml playbook"

  tasks:
    - name: Reset password
      ansible.builtin.include_role:
        name: site
        tasks_from: reset_password.yml

  # post_tasks:
  #   - include_role:
  #       name: etc-git
  #       tasks_from: commit.yml
  #     vars:
  #       commit_message: "Ansible {{ ansible_version.full }} post-run playbook.yml playbook"
